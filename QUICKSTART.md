# ⚡ Быстрый старт

Краткая инструкция для запуска бота за 10 минут.

## 1️⃣ Создайте Telegram бота (2 минуты)

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Придумайте имя и username (должен заканчиваться на `bot`)
4. **Скопируйте токен** (например: `1234567890:ABCdef...`)

## 2️⃣ Получите ID чата (1 минута)

1. Добавьте своего нового бота в групповой чат с партнером
2. Добавьте в этот же чат [@myidbot](https://t.me/myidbot)
3. Отправьте `/getgroupid`
4. **Скопируйте ID чата** (например: `-1001234567890`)

## 3️⃣ Получите токен Яндекс.Трекера (3 минуты)

### Вариант А: Быстрый (для теста)

1. Перейдите по ссылке: [Получить токен](https://oauth.yandex.ru/authorize?response_type=token&client_id=c0ebe342af7d48fbbbfcf2d2eedb8f9e)
2. Разрешите доступ
3. **Скопируйте токен из адресной строки**

### Вариант Б: Правильный (для продакшена)

См. раздел "Настройка Яндекс.Трекера" в README.md

## 4️⃣ Получите ID организации (1 минута)

**Способ 1: Из URL**
1. Откройте [Яндекс.Трекер](https://tracker.yandex.ru/)
2. ID организации в URL или в настройках

**Способ 2: Через API**
```bash
curl -H "Authorization: OAuth ВАШ_ТОКЕН" https://api.tracker.yandex.net/v2/myself
```
Найдите поле `orgId` в ответе.

## 5️⃣ Создайте очереди в Трекере (2 минуты)

1. Откройте [Яндекс.Трекер](https://tracker.yandex.ru/)
2. Создайте очереди:
   - **DEV** - для разработки
   - **MGR** - для менеджеров
   - **OPS** - для операционного отдела
   - **PARTNER1**, **PARTNER2** и т.д. - для каждого партнера

## 6️⃣ Установите бота (3 минуты)

1. Скачайте проект
2. Установите Python 3.8+ (если еще нет)
3. Установите зависимости:
```bash
pip install -r requirements.txt
```

## 7️⃣ Настройте конфигурацию (2 минуты)

### 7.1 Создайте файл `.env`

Скопируйте `.env.example` в `.env` и заполните:

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdef_ваш_токен
YANDEX_TRACKER_TOKEN=ваш_oauth_токен_яндекса
YANDEX_ORG_ID=12345
```

### 7.2 Настройте партнеров в `config.py`

Откройте `config.py` и найдите `PARTNER_CHAT_MAPPING`:

```python
PARTNER_CHAT_MAPPING: Dict[int, Dict[str, str]] = {
    -1001234567890: {  # ← Ваш ID чата
        'partner_name': 'Партнер Альфа',  # ← Название партнера
        'queue': 'PARTNER1',  # ← Очередь партнера в Трекере
    },
    # Добавьте других партнеров:
    # -1009876543210: {
    #     'partner_name': 'Партнер Бета',
    #     'queue': 'PARTNER2',
    # },
}
```

## 8️⃣ Запустите бота

```bash
python bot.py
```

Вы должны увидеть:
```
INFO - Запуск Telegram бота...
INFO - Бот запущен и готов к работе!
INFO - Настроено отделов: 3
INFO - Настроено партнеров: 1
```

## 9️⃣ Проверьте работу

1. Откройте чат с партнером в Telegram
2. Отправьте:
```
#задача Тестовая задача
```

3. Бот должен ответить с подтверждением и ссылкой на задачу

## ✅ Готово!

Теперь вы можете:
- Писать `#задача текст` для создания задач
- Добавлять `#dev`, `#mgr`, `#ops` для выбора отдела
- Использовать `/help` для просмотра всех команд

---

## 🆘 Не работает?

### Ошибка "Invalid token"
→ Проверьте `TELEGRAM_BOT_TOKEN` в `.env`

### Ошибка "401 Unauthorized" от Трекера
→ Проверьте `YANDEX_TRACKER_TOKEN` и `YANDEX_ORG_ID`

### Задача не создается в очереди партнера
→ Проверьте chat_id и название очереди в `config.py`

### Бот не отвечает в чате
→ Убедитесь, что бот добавлен в групповой чат

---

## 📚 Дополнительно

Для подробной информации смотрите [README.md](README.md)
