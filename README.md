# Telegram Tracker Bot

Бот для создания задач в Яндекс.Трекере из Telegram.

## Быстрый старт

1. **Создай очередь PARTNERS** (один раз):
   - Дважды кликни: `СОЗДАТЬ_PARTNERS.cmd`
   - Дождись: "✅ Очередь PARTNERS успешно создана!"

2. **Настрой менеджеров** в `config.py`:
```python
MANAGER_IDS = [
    8337630955,  # твой ID (узнай через /info боту)
]
```

3. **Настрой исполнителей** в `config.py`:
```python
PARTNER_ASSIGNEES = {
    '2': 'artemiy-starikov@yandex.ru',      # WEB#2 → artemiy
    '3': 'artemiy-starikov@yandex.ru',      # WEB#3 → artemiy
    # Добавляй новых партнеров здесь
}

DEFAULT_PARTNER_ASSIGNEE = 'artemiy-starikov@yandex.ru'  # для новых партнеров
```

4. **Запусти бота**: дважды кликни на `START_BOT.cmd`

## Как работает

**Менеджер** пишет в группе:
```
#задача WEB#25 Проблема с оплатой
```

**В группу** отправляется:
```
✅ Задача создана
📝 Проблема с оплатой
```

**Менеджеру в ЛС** отправляется:
```
✅ Задача создана успешно!
📝 Название: Проблема с оплатой
⚠️ Приоритет: critical
📅 Дедлайн: 2026-02-05
📋 PARTNERS-15 (Партнер WEB25)
🔗 https://tracker.yandex.ru/PARTNERS-15

[✅ Завершить задачу] ← кнопка
```

**При завершении задачи** (по кнопке):
```
✅ Задача выполнена!
📝 Проблема с оплатой
```

## Важно

- ✅ Только менеджеры (ID в `config.py`) могут создавать задачи
- ❌ Партнёры получат отказ при попытке создать задачу
- 🔘 Кнопка "Завершить" только у менеджеров
- 📋 Завершённые задачи убираются из списка
- 📢 Уведомление о завершении отправляется в чат

## Команды

### Для всех:
- `/start` - начало работы
- `/help` - справка
- `/info` - узнать свой ID и статус

### Для менеджеров:
- `/mytasks` - активные задачи чата
- `/partners` - список всех партнеров + количество задач
- `/partner WEB2` - задачи конкретного партнера

## Структура задач

- **PARTNERS** - очередь для всех партнерских задач
- **MNG** - очередь для менеджеров (#mgr)
- Задачи партнеров группируются по тегам: WEB2, WEB25, WEB123
- Автоматическое назначение исполнителя при создании задачи

## Как добавить нового партнера

В `config.py` добавь в `PARTNER_ASSIGNEES`:
```python
PARTNER_ASSIGNEES = {
    '2': 'artemiy-starikov@yandex.ru',
    '25': 'manager2@yandex.ru',  # <- новый партнер
}
```

Если партнера нет в списке - назначится `DEFAULT_PARTNER_ASSIGNEE`
