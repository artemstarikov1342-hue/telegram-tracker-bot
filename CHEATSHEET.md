# 📋 Шпаргалка

Краткая справка по использованию бота.

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу с ботом |
| `/help` | Показать справку |
| `/info` | Информация о чате и настройках |
| `/mytasks` | Список активных задач |

## Создание задач

### Базовый синтаксис
```
#задача Текст задачи
```

### С описанием
```
#задача Название задачи
Подробное описание задачи
Может быть многострочным
```

### С отделом
```
#задача #dev Исправить баг
```

### В несколько отделов
```
#задача #dev #ops Проблема с сервером
```

## Хештеги отделов

| Хештег | Отдел | Очередь |
|--------|-------|---------|
| `#dev` | Разработка | DEV |
| `#mgr` | Менеджер | MGR |
| `#ops` | Операционный | OPS |

### Синонимы (работают одинаково)

- `#dev` = `#разраб` = `#разработка`
- `#mgr` = `#менедж` = `#менеджер`
- `#ops` = `#операц` = `#операционный`

## Параметры по умолчанию

- **Приоритет**: Critical (критический)
- **Дедлайн**: Сегодня
- **Исполнитель**: Не назначен (или из настроек отдела)

## Завершение задачи

1. **Из Telegram**: Нажмите кнопку "✅ Завершить задачу"
2. **Из Трекера**: Измените статус на "Закрыта" / "Завершена"

## Примеры использования

### Простая задача
```
#задача Проверить логи за вчера
```

### Задача для разработчиков
```
#задача #dev Исправить баг #1234
При входе пользователь видит белый экран
```

### Срочная задача в несколько отделов
```
#задача #dev #ops Сервер недоступен
Prod сервер не отвечает на запросы
```

### Задача менеджерам
```
#задача #mgr Обновить договор с партнером
Срок действия истекает через 3 дня
```

## Что будет создано?

При отправке задачи создаются:

1. **Задача в указанных отделах** (если есть хештеги #dev, #mgr, #ops)
2. **Задача в очереди партнера** (автоматически по ID чата)
3. **Запись в базе данных** (для отслеживания)

### Пример

Если в чате партнера "Альфа" написать:
```
#задача #dev #ops Проблема с API
```

Будет создано **3 задачи**:
- `DEV-123` в очереди Разработки
- `OPS-456` в очереди Операционного отдела
- `PARTNER1-789` в очереди партнера "Альфа"

## Настройка

### Добавить партнера

В `config.py` в `PARTNER_CHAT_MAPPING`:

```python
-1001234567890: {
    'partner_name': 'Новый партнер',
    'queue': 'PARTNER3',
},
```

### Изменить приоритет

В `config.py`:

```python
DEFAULT_PRIORITY = 'normal'  # trivial, minor, normal, critical, blocker
```

### Изменить дедлайн

В `config.py`:

```python
DEFAULT_DEADLINE_DAYS = 1  # 0=сегодня, 1=завтра, 7=через неделю
```

### Назначить исполнителя

В `config.py` в `DEPARTMENT_MAPPING`:

```python
'dev': {
    'name': 'Разработка',
    'queue': 'DEV',
    'assignee': 'ivan.petrov',  # ← Логин в Трекере
    'hashtag': '#dev'
},
```

## Полезные ссылки

- [Яндекс.Трекер](https://tracker.yandex.ru/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [OAuth Яндекс](https://oauth.yandex.ru/)

## Файлы проекта

```
telegram_tracker_bot/
├── bot.py              - Основной файл бота
├── config.py           - Настройки (редактируйте здесь!)
├── .env                - Токены (НЕ коммитьте в git!)
├── tasks_db.json       - База данных задач
└── bot.log             - Логи работы бота
```

## Где посмотреть логи?

**Windows**: Откройте `bot.log` в папке проекта

**Linux**: 
```bash
tail -f bot.log
```

## Команды для разработчиков

### Тест подключения
```bash
python test_connection.py
```

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Запуск бота
```bash
python bot.py
```

### Проверка версии Python
```bash
python --version
```

## Быстрое решение проблем

| Проблема | Решение |
|----------|---------|
| Бот не отвечает | Проверьте, запущен ли `bot.py` |
| Ошибка токена | Проверьте `.env` файл |
| Задача не создается | Проверьте очередь в Трекере |
| Нет прав | Дайте права на очередь в Трекере |
| Бот не видит сообщения | Добавьте бота в групповой чат |

## Контакты и поддержка

Проблемы? Смотрите:
1. `README.md` - Подробная документация
2. `QUICKSTART.md` - Быстрый старт
3. `bot.log` - Логи ошибок

---

**Версия**: 1.0 | **Дата**: 2026
