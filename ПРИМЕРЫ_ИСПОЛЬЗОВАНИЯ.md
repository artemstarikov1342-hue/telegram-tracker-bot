# 📖 Примеры использования бота

## 🆔 Новая система: WEB#ID

Теперь **партнер сам указывает свой ID** в сообщении!

---

## ✅ Пример 1: Партнер создает задачу

**Партнер пишет:**
```
#задача WEB#25 Не работает форма заказа на сайте
```

**Что происходит:**
1. Бот видит `WEB#25`
2. Создает очередь `WEB25` (если еще нет)
3. Создает задачу в `WEB25`
4. Партнер видит простое сообщение:
   ```
   ✅ Задача создана!
   
   📝 Не работает форма заказа на сайте
   ```

---

## ✅ Пример 2: Менеджер создает задачу для партнера

**Менеджер пишет:**
```
#задача WEB#25 Проблема с оплатой у клиента
```

**Что происходит:**
1. Задача создается в очереди `WEB25`
2. Менеджер видит подробное сообщение с кнопкой:
   ```
   ✅ Задача создана успешно!
   
   📝 Название: Проблема с оплатой у клиента
   ⚠️ Приоритет: critical
   📅 Дедлайн: 2026-02-03
   
   1. 📋 WEB25-5 (Партнер)
      🔗 https://tracker.yandex.ru/WEB25-5
   
   [✅ Завершить задачу]
   ```

---

## ✅ Пример 3: Задача для партнера + отдел менеджеров

**Менеджер пишет:**
```
#задача WEB#25 #mgr Срочная проблема с партнером
Требуется консультация
```

**Что происходит:**
1. Задача создается в `WEB25` (партнер)
2. И ТАКЖЕ в `MNG` (менеджеры)
3. Менеджер видит обе задачи:
   ```
   ✅ Задача создана успешно!
   
   📝 Название: Срочная проблема с партнером
   ⚠️ Приоритет: critical
   📅 Дедлайн: 2026-02-03
   
   1. 📋 MNG-12 (Менеджер)
      🔗 https://tracker.yandex.ru/MNG-12
   
   2. 📋 WEB25-6 (Партнер)
      🔗 https://tracker.yandex.ru/WEB25-6
   
   [✅ Завершить задачу]
   ```

---

## ✅ Пример 4: Внутренняя задача (только отдел)

**Менеджер пишет:**
```
#задача #mgr Подготовить отчет для руководства
```

**Что происходит:**
1. Задача создается только в `MNG`
2. Без привязки к партнеру

---

## ✅ Пример 5: Завершение задачи

**Менеджер нажимает кнопку "✅ Завершить задачу"**

**Что происходит:**
1. Задача закрывается в Яндекс.Трекере
2. В чат приходит уведомление (видят ВСЕ):
   ```
   ✅ Задача выполнена!
   
   📝 Проблема с оплатой у клиента
   ```

---

## 🔑 Ключевые возможности

### Для менеджеров (ID в MANAGER_IDS):
- ✅ Видят полную информацию о задаче
- ✅ Видят кнопку "Завершить задачу"
- ✅ Могут создавать задачи в любых очередях

### Для партнеров:
- ✅ Видят простое сообщение "Задача создана"
- ✅ Не видят технических деталей
- ❌ Не могут завершать задачи через кнопку

---

## 🎯 Форматы ID партнеров

Поддерживаются любые числа после `WEB#`:

- `WEB#1` → очередь `WEB1`
- `WEB#25` → очередь `WEB25`
- `WEB#123` → очередь `WEB123`
- `WEB#9999` → очередь `WEB9999`

---

## ⚠️ Важные правила

1. **ID партнера** должен быть указан в формате `WEB#число`
2. **Очередь создается автоматически** при первой задаче
3. **Кнопка "Завершить"** видна только менеджерам
4. **WEB#ID удаляется** из текста задачи (остается только для маршрутизации)

---

## 🚀 Быстрый старт

1. Узнай свой Telegram ID:
   ```
   /info
   ```

2. Добавь свой ID в `config.py`:
   ```python
   MANAGER_IDS = [
       123456789,  # твой ID
   ]
   ```

3. Перезапусти бота

4. Протестируй:
   ```
   #задача WEB#123 Тестовая задача
   ```

---

## 📞 Команды бота

- `/start` - Информация о боте
- `/help` - Справка по использованию
- `/info` - Информация о чате и твой ID
- `/mytasks` - Активные задачи чата
